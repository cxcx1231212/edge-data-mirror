# Cloudflare mirror

This Worker transparently proxies the existing BaoTa-hosted site at
`https://lhtxptlt.235-from.com`. The PHP application and database remain on the
origin server; no existing files or DNS records are modified.

Deploy with Wrangler from this directory.
